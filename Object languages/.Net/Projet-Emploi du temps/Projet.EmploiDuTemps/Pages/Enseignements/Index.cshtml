@page
@model Projet.EmploiDuTemps.Pages.Enseignements.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Enseignement[0].GroupeID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Enseignement[0].UEID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Enseignement) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UEID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
